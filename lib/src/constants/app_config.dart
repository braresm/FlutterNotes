class AppConfig {
  // How many colums will have the calculator keyboard
  static int keyboardColumnsNumber = 4;

  // Allowed operators for calculation
  static List<String> allowedOperators = ['+', '-', '*', '/'];
}
